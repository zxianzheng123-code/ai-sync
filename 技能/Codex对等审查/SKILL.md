---
description: 当需要让 Codex (GPT-5.4) 对 Claude 的方案或代码做对等审查、获取独立第二视角时，使用此技能
trigger: Codex审查
version: 2.0
---

## 执行模式
Master Agent 负责调度和验收，不亲自干活。每个步骤启动 SubAgent 执行。

## 工作人员
- 审查官（→ 技能/人设库/审查官/人设.md，本技能中负责组织审查材料、与 Codex 对话、整合反馈）

## Workflow

### Step 0：加载上下文（Master Agent）
执行 脚本/加载上下文.sh

### Step 1：确认审查对象
- 执行者：Master Agent（需与用户交互）
- 目标：提取上一轮完整输出，向用户确认审查范围
- 成功标准：用户明确确认了要送审的内容
- 输入：本次对话中最近一轮的完整回复
- 输出：确认后的审查对象全文

流程：
1. 提取上一轮回复的完整内容，原样保留，不总结、不筛选、不省略
2. 向用户展示前 5 行或前 200 字的摘要（取短的那个）
3. 提示用户：「这是要审查的内容吗？有异议请说明，否则直接回复继续。」
4. 收到用户确认后标记完成

进度显示：
```
[Step 1 开始] 确认审查对象
📋 即将送审的内容：
（前几行摘要）
...
```

### Step 2：发送 Codex 首轮审查
- 执行者：审查官（SubAgent）
- 目标：将审查对象发送给 Codex，获取首轮独立审查意见
- 成功标准：Codex 返回了包含具体意见的结构化反馈（非空、非报错）
- 输入：Step 1 确认的审查对象全文
- 输出：Codex 首轮审查意见

调用方式（直接用 heredoc 喂 stdin，不写临时文件）：
```bash
codex exec -m gpt-5.4 -c model_reasoning_effort="xhigh" - <<'PROMPT_EOF'
你是一位资深工程师，正在和同事进行对等技术讨论。请用中文回复。

同事给出了以下方案：

---
[审查对象全文]
---

请你：
1. 自行查看项目中的相关源码、配置和测试，形成你自己的独立判断
2. 用 ✅ 标记你同意的要点
3. 用 ❌ 标记你不同意的要点，给出理由和替代建议
4. 简洁回复，聚焦最关键的 2-3 个问题
PROMPT_EOF
```

异常处理：
- 若 codex exec 返回非零退出码、超时或输出为空 → 向 Master Agent 报告错误信息，由 Master Agent 询问用户是否重试
- 最多重试 2 次，仍失败则终止技能并告知用户排查

进度显示：
```
[Step 2 开始] 正在发送给 Codex (GPT-5.4 xhigh)，等待审查回复...
📨 Codex 已回复。以下是 Codex 的审查意见：
```

### Step 3：对等讨论
- 执行者：审查官（SubAgent）
- 目标：逐条回应 Codex 反馈，同意的采纳，不同意的说明理由并继续讨论，最终收敛分歧
- 成功标准：所有意见已明确处理（采纳或保留分歧理由）；讨论在 3 轮内完成
- 输入：Step 2 的 Codex 审查意见 + 原始方案
- 输出：讨论结论（采纳清单 + 分歧清单 + 修改要点）

讨论规则：
- Claude 和 Codex 是平等同事，不是上下级
- Codex 的意见是参考，不是否决权
- 不为和谐妥协技术判断——有充分理由时坚持原方案
- 最多 3 轮讨论

每轮流程：
1. 逐条标记同意（采纳）或不同意（附理由）
2. 将回应通过 heredoc 发给 Codex：
```bash
codex exec -m gpt-5.4 -c model_reasoning_effort="xhigh" - <<'PROMPT_EOF'
你是一位资深工程师，正在和同事进行对等技术讨论。请用中文回复。

上一轮你提出了这些意见，我的回应如下：

---
[逐条回应内容]
---

请确认你是否接受我的回应，或继续讨论有分歧的点。
PROMPT_EOF
```
3. 收到回复后判断：所有分歧已解决 → 进入 Step 4；仍有分歧且未满 3 轮 → 继续下一轮

3 轮后仍有分歧的退出策略：
- 输出分歧摘要，标注双方立场和理由
- 交由用户做最终裁决
- 用户裁决后进入 Step 4

透明度要求——每轮讨论结束后向用户输出简短摘要：
```
🔄 第 N/3 轮讨论完成：
- 已采纳：X 条
- 仍有分歧：Y 条（简述分歧内容）
```

### Step 4：整合并提交最终确认
- 执行者：审查官（SubAgent）
- 目标：将讨论共识整合为修改后的完整方案，提交 Codex 做最终确认
- 成功标准：Codex 明确回复「✅ 通过」或用户完成裁决
- 输入：Step 3 的讨论结论 + 原始方案
- 输出：经确认的最终方案

提交方式：
```bash
codex exec -m gpt-5.4 -c model_reasoning_effort="xhigh" - <<'PROMPT_EOF'
你是一位资深工程师。经过之前几轮讨论，同事修改了方案。请用中文回复。

请审查这份修改后的最终版本：

---
[修改后的完整方案]
---

请确认：
1. 之前提出的问题是否已经妥善解决
2. 修改是否引入了新的问题
3. 如果全部通过，请明确回复「✅ 通过」
4. 如果仍有问题，指出具体问题
PROMPT_EOF
```

打回机制：
- Codex 回复通过 → 进入 Step 5
- Codex 仍有问题 → 回到 Step 3 继续讨论（Step 3 的轮次计数器重置）
- 最多被打回 3 次
- 第 3 次仍未通过 → 输出 Codex 不满意的点和自己的立场，交用户裁决

### Step 5：验证
- 如有 脚本/验证/ 下的脚本 → 自动执行
- 如有 作业指引/交付自查清单.md → 逐项检查
- 额外检查项：
  - [ ] 审查对象与用户确认的一致
  - [ ] Codex 所有意见均已明确处理（采纳或保留理由）
  - [ ] 最终方案完整，无遗漏章节
  - [ ] 变更说明准确反映了修改内容
- 全部通过才进入交付

### Step 6：交付审核（Master Agent）
输出最终方案，附变更说明（标注哪些地方因 Codex 反馈修改，哪些地方坚持了原方案及理由），等用户确认。

汇报内容：
- 完整的最终方案或代码
- 变更说明
- 讨论轮次统计（共 N 轮，采纳 X 条，坚持 Y 条）
