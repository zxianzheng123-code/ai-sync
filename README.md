# AI Tool Library

## 仓库定位

这是一个**个人自定义 AI 工具库**。

定位只有一句话：

**面向个人使用，持续沉淀可复用的 AI 技能、规则和方法资产，工具类型不设限制。**

这个仓库不是单一项目代码仓，也不是单一工作流产品。

它的核心职责是：

- 存放可复用的技能
- 存放全局规则
- 存放少量仓库级方法文档

## 使用方法

推荐使用方式只有一条：

**把 `AGENTS.md` 复制到任何项目根目录，即可把这套工具使用方式带进那个项目。**

这里的分工是：

- `AGENTS.md` 是对外使用入口
- 当前仓库是 `AGENTS.md`、规则和技能的维护源头

## 根目录文件架构

根目录只表达**推荐结构**，不在这里枚举具体 Skill 目录：

```text
ai-sync/
├── README.md                         # 仓库总说明：定位、结构、维护原则
├── AGENTS.md                         # 可复制到任意项目的使用入口
├── 规则/                            # 本项目构建者使用的内部规则
│   └── 通用文档写作规则.md
├── 技能/                           # 全部技能，保持扁平
│   └── <技能目录名>/
│       ├── SKILL.md
│       ├── 参考资料/
│       ├── 脚本/
│       └── 资源/
└── <repo-level-docs>.md              # 少量仓库级专题文档或讨论文档
```

## 工具一览表

当前仓库里的工具，统一按“可主动触发的 Skill”理解。

这里把**一级工具**、**二级入口**和**三级实际子 Skill**一起列出。  
其中“目录中文名”与“实际目录”默认保持同一套中文口径。

| 一级工具                  | 二级入口                 | 三级实际子 Skill     | 目录中文名                   | 实际目录                                                       | 类型   | 用途                          | 触发方式 |
| --------------------- | -------------------- | --------------- | ----------------------- | ---------------------------------------------------------- | ---- | --------------------------- | ---- |
| `规则加载器`               | —                    | —               | `规则加载器`                 | `技能/规则加载器/`                                       | 系统规则 | 读取并施加全局规则                   | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `1-业务上下文压缩师`    | `流水线 / 阶段 1 / 业务上下文压缩`  | `技能/流水线指挥中心/阶段/1-业务上下文压缩师/` | 流程调度 | 产出 `2+4_project_context.md` | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `2-布局蓝图与风格册翻译官` | `流水线 / 阶段 2 / 布局蓝图与风格册` | `技能/流水线指挥中心/阶段/2-布局蓝图与风格册翻译官/`    | 流程调度 | 产出布局蓝图与风格册                  | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `3-静态界面画师`      | `流水线 / 阶段 3 / 静态界面画师`   | `技能/流水线指挥中心/阶段/3-静态界面画师/`  | 流程调度 | 产出静态高保真 `.pen` 稿            | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `4-环境基建说明书生成师`  | `流水线 / 阶段 4 / 环境基建说明书`  | `技能/流水线指挥中心/阶段/4-环境基建说明书生成师/`         | 流程调度 | 产出 `infra_spec.md` 和项目骨架    | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `5-动态 UI 交互专家组` | `流水线 / 阶段 5 / 动态 UI 交互` | `技能/流水线指挥中心/阶段/5-动态UI交互专家组/` | 流程调度 | 产出交互原型和交互地图                 | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `6a-代码蓝图师`      | `流水线 / 阶段 6a / 代码蓝图`    | `技能/流水线指挥中心/阶段/6a-代码蓝图师/`   | 流程调度 | 产出前后端代码蓝图                   | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `6b-逐页编码师`      | `流水线 / 阶段 6b / 逐页编码`    | `技能/流水线指挥中心/阶段/6b-逐页编码师/`      | 流程调度 | 按页写前端生产代码                   | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `6c-全局集成师`      | `流水线 / 阶段 6c / 全局集成`    | `技能/流水线指挥中心/阶段/6c-全局集成师/`       | 流程调度 | 完成路由、状态和应用集成                | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `7-后端编码师`       | `流水线 / 阶段 7 / 后端编码`     | `技能/流水线指挥中心/阶段/7-后端编码师/`    | 流程调度 | 编写后端并完成前后端联调                | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `8a-部署方案设计师`    | `流水线 / 阶段 8a / 部署方案设计`  | `技能/流水线指挥中心/阶段/8a-部署方案设计师/`      | 流程调度 | 产出部署方案文档                    | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `8b-部署执行师`      | `流水线 / 阶段 8b / 部署执行`    | `技能/流水线指挥中心/阶段/8b-部署执行师/`       | 流程调度 | 执行部署并产出可访问环境                | 显式触发 |
| `流水线指挥中心`             | `流水线推进`              | `9-验收签收师`       | `流水线 / 阶段 9 / 验收签收`     | `技能/流水线指挥中心/阶段/9-验收签收师/`      | 流程调度 | 做最终验收、回流和签收                 | 显式触发 |
| `流水线指挥中心`             | `运维排查（troubleshoot）` | `故障诊修师`         | `流水线 / 运维 / 排查`         | `技能/流水线指挥中心/运维/故障诊修师/`                | 流程调度 | 诊断并修复指定范围 bug               | 显式触发 |
| `流水线指挥中心`             | `运维重构（refactor）`     | `代码重构师`         | `流水线 / 运维 / 重构`         | `技能/流水线指挥中心/运维/代码重构师/`                    | 流程调度 | 重组代码结构且不改外部行为               | 显式触发 |
| `流水线指挥中心`             | `运维优化（optimize）`     | `性能优化师`         | `流水线 / 运维 / 优化`         | `技能/流水线指挥中心/运维/性能优化师/`                    | 流程调度 | 优化性能、体验或代码质量                | 显式触发 |
| `流水线指挥中心`             | `运维测试（test）`         | `质量测试师`         | `流水线 / 运维 / 测试`         | `技能/流水线指挥中心/运维/质量测试师/`                        | 流程调度 | 编写、补充并运行自动化测试               | 显式触发 |
| `PLAYBOOK编写师`         | —                    | —               | `PLAYBOOK 编写`           | `技能/PLAYBOOK编写师/`                          | 方法文档 | 从口述中整理 PLAYBOOK             | 显式触发 |
| `Skill编译器`            | —                    | —               | `Skill 编译`              | `技能/Skill编译器/`                           | 方法文档 | 新建或修订 `SKILL.md`            | 显式触发 |
| `服装图片商品化`             | —                    | —               | `服装图片商品化`               | `技能/服装图片商品化/`                    | 图片处理 | 服装图片商品化处理                   | 显式触发 |
| `Obsidian Markdown编辑` | —                    | —               | `Obsidian Markdown 编辑`  | `技能/Obsidian-Markdown编辑/`                                | 内容编辑 | 编辑 Obsidian Markdown 笔记     | 显式触发 |
| `Obsidian Bases编辑`    | —                    | —               | `Obsidian Bases 编辑`     | `技能/Obsidian-Bases编辑/`                                   | 内容编辑 | 编辑 Obsidian Bases（`.base`）  | 显式触发 |
| `JSON Canvas编辑`       | —                    | —               | `JSON Canvas 编辑`        | `技能/JSON-Canvas编辑/`                                      | 内容编辑 | 编辑 JSON Canvas（`.canvas`）   | 显式触发 |
| `Obsidian CLI`        | —                    | —               | `Obsidian CLI`          | `技能/Obsidian命令行/`                                     | 工具操作 | 通过命令行操作 Obsidian vault      | 显式触发 |
| `网页正文提取`              | —                    | —               | `网页正文提取`                | `技能/网页正文提取/`                                         | 抓取清洗 | 从网页提取干净 Markdown 正文         | 显式触发 |

## 各层职责

| 层   | 对象          | 职责                        | 边界                |
| --- | ----------- | ------------------------- | ----------------- |
| 1   | `README.md` | 解释仓库定位、推荐结构、维护原则          | 不写具体 Skill 正文     |
| 2   | `AGENTS.md` | 提供可复制到任意项目的 Skill 入口与触发规则 | 不替代 `SKILL.md` 正文 |
| 3   | `规则/`    | 存放本项目构建者使用的内部规则；当前写作规则文件是 `规则/通用文档写作规则.md` | 不作为项目使用者入口 |
| 4   | `技能/`   | 存放全部可复用 Skill             | 不混入无关的一次性项目资料     |

`技能/` 层补充规则：

1. 一项技能一个目录。
2. 尽量保持扁平，不做深层分类目录。
3. 技能内部只放和技能直接相关的内容。

## 技能组织原则

这个仓库后续不按“项目类型”统一收口，而按“可复用能力”持续增加。

当前已经有四类能力：

- 流程与方法：`流水线指挥中心`、`PLAYBOOK编写师`、`Skill编译器`、`规则加载器`
- 内容与文档：`Obsidian Markdown编辑`、`Obsidian Bases编辑`、`JSON Canvas编辑`
- 工具与抓取：`Obsidian CLI`、`网页正文提取`
- 专题处理：`服装图片商品化`

后续新增 Skill 时，优先遵守这两个判断：

1. 这是可复用能力，还是一次性项目资料？
2. 这是独立 Skill，还是现有 Skill 的参考附件？

如果只是一次性项目资料，不进入 `技能/`。

## 维护规则

- 新 Skill 一律放在 `技能/` 根下，不单独再建二级分类目录。
- 是否能被触发，以 `AGENTS.md` 为准。
- Skill 正文写在 `SKILL.md`，长说明放进 `参考资料/`。
- 本项目构建与维护时的写作规则，以 `规则/通用文档写作规则.md` 为准。
- 仓库级讨论、定位、迁移说明，优先写进根目录或后续单独归档，不混进 Skill 目录。

## 触发原则

现阶段这个仓库里的内容，统一按**主动触发**理解。

这里的主动触发，指的是：

- 由用户明确点名某个 Skill
- 或用户给出明确动作指令，等价于点名某个 Skill

这里不采用被动触发，指的是：

- 不因为语义“有点像”就自动启用某个 Skill
- 不因为仓库里存在某个 Skill 就默认接管任务

原因很简单：

这个工具库还不成熟，技能边界、文档口径和适用范围都还在收敛阶段。

所以当前规则应当保守：

**宁可少触发，也不要误触发。**

## 当前状态

当前仓库已经从“单项目流水线”回退为“个人 AI 工具库”方向。

也就是说，后面的重点不再是继续堆实现，而是先把：

- 技能库结构
- 规则体系
- 技能边界

这三件事收稳。
